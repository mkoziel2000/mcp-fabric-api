# Transport mode: "stdio" (default) or "http"
TRANSPORT=stdio

# HTTP mode settings
PORT=3000

# Required for HTTP mode (OAuth2 with Azure Entra ID)
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
